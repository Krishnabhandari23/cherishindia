{
  "name": "cherish-india-fullstack",
  "version": "1.0.0",
  "description": "Cherish India E-Commerce Full Stack Application",
  "main": "server/server.js",
  "engines": {
    "node": "18.x || 20.x || 22.x",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "build": "npm install --prefix server && cd client && npm ci && npm run build",
    "start": "npm start --prefix server",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install-client": "npm install --prefix client",
    "install-server": "npm install --prefix server",
    "build-client": "npm run build --prefix client",
    "heroku-postbuild": "npm run install-client && npm run build-client && npm run install-server"
  },
  "keywords": [
    "ecommerce",
    "fullstack",
    "react",
    "node",
    "express",
    "mongodb"
  ],
  "author": "Krishnabhandari23",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}